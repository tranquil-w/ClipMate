<UserControl
    x:Class="ClipMate.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    mc:Ignorable="d">

    <UserControl.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CloseCommand}" />
    </UserControl.InputBindings>

    <Grid Background="{DynamicResource RegionBrush}">
        <ScrollViewer
            Margin="0,50,0,0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <Grid MaxWidth="800" Margin="40,0,40,40">
                <StackPanel>
                    <!--  主题设置卡片  -->
                    <Border Style="{StaticResource SettingsCardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SettingsTitleStyle}" Text="外观主题" />

                            <TextBlock
                                Margin="0,0,0,20"
                                Style="{StaticResource SettingsDescriptionStyle}"
                                Text="选择适合你的界面风格" />

                            <!--  主题选项卡片  -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!--  浅色主题  -->
                                <Border
                                    Grid.Column="0"
                                    Margin="0,0,8,0"
                                    Cursor="Hand">
                                    <Border.Style>
                                        <Style BasedOn="{StaticResource ThemeCardStyle}" TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLightTheme}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                                    <Setter Property="BorderThickness" Value="2" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.InputBindings>
                                        <MouseBinding Command="{Binding SelectLightThemeCommand}" MouseAction="LeftClick" />
                                    </Border.InputBindings>
                                    <StackPanel>
                                        <!--  浅色主题预览  -->
                                        <Border Style="{StaticResource ThemePreviewLightStyle}">
                                            <Grid>
                                                <StackPanel
                                                    Margin="8"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top">
                                                    <Rectangle
                                                        Width="30"
                                                        Height="4"
                                                        Margin="0,0,0,4"
                                                        Fill="#333333"
                                                        RadiusX="2"
                                                        RadiusY="2" />
                                                    <Rectangle
                                                        Width="40"
                                                        Height="3"
                                                        Fill="#999999"
                                                        RadiusX="1.5"
                                                        RadiusY="1.5" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                        <TextBlock Style="{StaticResource ThemeCardLabelStyle}" Text="浅色" />
                                    </StackPanel>
                                </Border>

                                <!--  深色主题  -->
                                <Border
                                    Grid.Column="1"
                                    Margin="4,0,4,0"
                                    Cursor="Hand">
                                    <Border.Style>
                                        <Style BasedOn="{StaticResource ThemeCardStyle}" TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkTheme}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                                    <Setter Property="BorderThickness" Value="2" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.InputBindings>
                                        <MouseBinding Command="{Binding SelectDarkThemeCommand}" MouseAction="LeftClick" />
                                    </Border.InputBindings>
                                    <StackPanel>
                                        <!--  深色主题预览  -->
                                        <Border Style="{StaticResource ThemePreviewDarkStyle}">
                                            <Grid>
                                                <StackPanel
                                                    Margin="8"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top">
                                                    <Rectangle
                                                        Width="30"
                                                        Height="4"
                                                        Margin="0,0,0,4"
                                                        Fill="#FFFFFF"
                                                        RadiusX="2"
                                                        RadiusY="2" />
                                                    <Rectangle
                                                        Width="40"
                                                        Height="3"
                                                        Fill="#999999"
                                                        RadiusX="1.5"
                                                        RadiusY="1.5" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                        <TextBlock Style="{StaticResource ThemeCardLabelStyle}" Text="深色" />
                                    </StackPanel>
                                </Border>

                                <!--  跟随系统  -->
                                <Border
                                    Grid.Column="2"
                                    Margin="8,0,0,0"
                                    Cursor="Hand">
                                    <Border.Style>
                                        <Style BasedOn="{StaticResource ThemeCardStyle}" TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSystemTheme}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                                    <Setter Property="BorderThickness" Value="2" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.InputBindings>
                                        <MouseBinding Command="{Binding SelectSystemThemeCommand}" MouseAction="LeftClick" />
                                    </Border.InputBindings>
                                    <StackPanel>
                                        <!--  跟随系统主题预览  -->
                                        <Border
                                            Height="80"
                                            BorderBrush="#999999"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Border
                                                    Grid.Column="0"
                                                    Background="White"
                                                    CornerRadius="4,0,0,4">
                                                    <StackPanel
                                                        Margin="4"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Top">
                                                        <Rectangle
                                                            Width="15"
                                                            Height="3"
                                                            Margin="0,0,0,2"
                                                            Fill="#333333"
                                                            RadiusX="1.5"
                                                            RadiusY="1.5" />
                                                        <Rectangle
                                                            Width="20"
                                                            Height="2"
                                                            Fill="#999999"
                                                            RadiusX="1"
                                                            RadiusY="1" />
                                                    </StackPanel>
                                                </Border>
                                                <Border
                                                    Grid.Column="1"
                                                    Background="#1E1E1E"
                                                    CornerRadius="0,4,4,0">
                                                    <StackPanel
                                                        Margin="4"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Top">
                                                        <Rectangle
                                                            Width="15"
                                                            Height="3"
                                                            Margin="0,0,0,2"
                                                            Fill="#FFFFFF"
                                                            RadiusX="1.5"
                                                            RadiusY="1.5" />
                                                        <Rectangle
                                                            Width="20"
                                                            Height="2"
                                                            Fill="#999999"
                                                            RadiusX="1"
                                                            RadiusY="1" />
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                        </Border>
                                        <TextBlock Style="{StaticResource ThemeCardLabelStyle}" Text="跟随系统" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!--  快捷键设置卡片  -->
                    <Border Style="{StaticResource SettingsCardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SettingsTitleStyle}" Text="快捷键设置" />

                            <TextBlock
                                Margin="0,0,0,16"
                                Style="{StaticResource SettingsDescriptionStyle}"
                                Text="自定义显示/隐藏窗口的快捷键组合" />

                            <Grid
                                x:Name="HotKeyGrid"
                                Margin="0,8,0,0"
                                Background="Transparent"
                                Focusable="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Style="{StaticResource SettingsHotKeyDisplayStyle}">
                                    <Grid>
                                        <TextBlock Style="{StaticResource SettingsHotKeyPlaceholderStyle}" Text="点击录制按钮后按下快捷键" />
                                        <TextBlock
                                            x:Name="HotKeyTextBox"
                                            Style="{StaticResource SettingsHotKeyTextStyle}"
                                            Text="{Binding CurrentHotKey}" />
                                    </Grid>
                                </Border>

                                <Button
                                    x:Name="RecordHotKeyButton"
                                    Grid.Column="1"
                                    Width="100"
                                    Height="44"
                                    Margin="12,0,0,0"
                                    Command="{Binding RecordHotKeyCommand}"
                                    Content="录制"
                                    FontSize="14"
                                    Style="{DynamicResource ButtonPrimary}" />
                            </Grid>

                            <TextBlock
                                Margin="0,20,0,0"
                                FontSize="14"
                                FontWeight="Medium"
                                Foreground="{DynamicResource PrimaryTextBrush}"
                                Text="收藏筛选快捷键" />
                            <TextBlock
                                Margin="0,4,0,8"
                                FontSize="12"
                                Foreground="{DynamicResource SecondaryTextBrush}"
                                Text="快速切换仅显示收藏的过滤状态" />

                            <Grid
                                x:Name="FavoriteFilterHotKeyGrid"
                                Margin="0,0,0,0"
                                Background="Transparent"
                                Focusable="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Style="{StaticResource SettingsHotKeyDisplayStyle}">
                                    <Grid>
                                        <TextBlock Style="{StaticResource SettingsHotKeyPlaceholderStyle}" Text="点击录制按钮后按下快捷键" />
                                        <TextBlock
                                            Style="{StaticResource SettingsHotKeyTextStyle}"
                                            Text="{Binding FavoriteFilterHotKey}" />
                                    </Grid>
                                </Border>

                                <Button
                                    Grid.Column="1"
                                    Width="100"
                                    Height="44"
                                    Margin="12,0,0,0"
                                    Command="{Binding RecordFavoriteFilterHotKeyCommand}"
                                    Content="录制"
                                    FontSize="14"
                                    Style="{DynamicResource ButtonPrimary}" />
                            </Grid>

                            <TextBlock
                                Margin="0,12,0,0"
                                FontSize="12"
                                Foreground="{DynamicResource PrimaryTextBrush}"
                                Text="💡 支持 Ctrl、Alt、Shift + 字母/数字/功能键组合" />
                        </StackPanel>
                    </Border>

                    <!--  系统设置卡片  -->
                    <Border Style="{StaticResource SettingsCardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SettingsTitleStyle}" Text="系统设置" />

                            <TextBlock
                                Margin="0,0,0,20"
                                Style="{StaticResource SettingsDescriptionStyle}"
                                Text="配置应用启动和运行行为" />

                            <!--  管理员权限  -->
                            <StackPanel Margin="0,8,0,12">
                                <TextBlock
                                    FontSize="14"
                                    FontWeight="Medium"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="管理员权限" />
                                <TextBlock
                                    Margin="0,4,0,0"
                                    FontSize="12"
                                    Foreground="{DynamicResource SecondaryTextBrush}"
                                    Text="管理员模式下自启动将使用任务计划程序以保持权限" />
                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Style="{StaticResource SettingsHotKeyDisplayStyle}">
                                        <TextBlock Text="{Binding AdminStatusText}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="VerticalAlignment" Value="Center" />
                                                    <Setter Property="FontFamily" Value="Consolas" />
                                                    <Setter Property="FontSize" Value="16" />
                                                    <Setter Property="FontWeight" Value="Medium" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRunningAsAdmin}" Value="True">
                                                            <Setter Property="Foreground" Value="#2E7D32" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>

                                    <Button
                                        Grid.Column="1"
                                        Height="44"
                                        Margin="12,0,0,0"
                                        Command="{Binding RestartAsAdminCommand}"
                                        Content="以管理员身份重启"
                                        FontSize="14"
                                        Style="{DynamicResource ButtonPrimary}"
                                        Visibility="{Binding IsRunningAsAdmin, Converter={StaticResource BooleanToVisibilityReConverter}}" />
                                </Grid>
                            </StackPanel>

                            <!--  始终以管理员身份运行  -->
                            <StackPanel Margin="0,12,0,0">
                                <CheckBox
                                    Content="始终以管理员身份运行"
                                    FontSize="13"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    IsChecked="{Binding IsAlwaysRunAsAdmin}"
                                    IsEnabled="{Binding IsRunningAsAdmin}" />
                                <TextBlock
                                    Margin="22,4,0,0"
                                    FontSize="12"
                                    Foreground="{DynamicResource SecondaryTextBrush}"
                                    Text="仅当以管理员身份运行时，才能更改此设置" />
                            </StackPanel>

                            <Separator Margin="0,12,0,12" />

                            <!--  开机自启动设置  -->
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="Medium"
                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                        Text="开机自启动" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="12"
                                        Foreground="{DynamicResource SecondaryTextBrush}"
                                        Text="开机时自动启动 ClipMate" />
                                    <TextBlock
                                        Margin="0,6,0,0"
                                        FontSize="12"
                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                        Text="{Binding AutoStartMethodText}" />
                                </StackPanel>

                                <ToggleButton
                                    Grid.Column="1"
                                    Width="50"
                                    Height="26"
                                    IsChecked="{Binding IsAutoStartEnabled}"
                                    Style="{StaticResource ToggleButtonSwitch}" />
                            </Grid>

                            <Separator Margin="0,12,0,12" />

                            <!--  静默启动设置  -->
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="Medium"
                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                        Text="静默启动" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="12"
                                        Foreground="{DynamicResource SecondaryTextBrush}"
                                        Text="启动时最小化到托盘，不显示主窗口" />
                                </StackPanel>

                                <ToggleButton
                                    Grid.Column="1"
                                    Width="50"
                                    Height="26"
                                    IsChecked="{Binding IsSilentStart}"
                                    Style="{StaticResource ToggleButtonSwitch}" />
                            </Grid>

                            <Separator Margin="0,12,0,12" />

                            <!--  日志级别设置  -->
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="Medium"
                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                        Text="日志级别" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="12"
                                        Foreground="{DynamicResource SecondaryTextBrush}"
                                        Text="控制文件与调试输出的最小记录级别" />
                                </StackPanel>

                                <ComboBox
                                    Grid.Column="1"
                                    Width="160"
                                    ItemsSource="{Binding LogLevels}"
                                    SelectedValue="{Binding SelectedLogLevel}"
                                    SelectedValuePath="Level"
                                    DisplayMemberPath="DisplayName" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!--  界面设置卡片  -->
                    <Border Style="{StaticResource SettingsCardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SettingsTitleStyle}" Text="界面设置" />

                            <TextBlock
                                Margin="0,0,0,20"
                                Style="{StaticResource SettingsDescriptionStyle}"
                                Text="自定义剪贴列表的显示效果" />

                            <!--  窗口显示位置  -->
                            <StackPanel Margin="0,8,0,20">
                                <TextBlock
                                    FontSize="14"
                                    FontWeight="Medium"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="窗口显示位置" />
                                <TextBlock
                                    Margin="0,4,0,0"
                                    FontSize="12"
                                    Foreground="{DynamicResource SecondaryTextBrush}"
                                    Text="选择窗口显示的位置" />

                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border
                                        Grid.Column="0"
                                        Margin="0,0,8,0"
                                        Cursor="Hand">
                                        <Border.Style>
                                            <Style BasedOn="{StaticResource ThemeCardStyle}" TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFollowCaret}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                                        <Setter Property="BorderThickness" Value="2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.InputBindings>
                                            <MouseBinding Command="{Binding SelectFollowCaretCommand}" MouseAction="LeftClick" />
                                        </Border.InputBindings>
                                        <StackPanel Margin="12">
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="Medium"
                                                Foreground="{DynamicResource PrimaryTextBrush}"
                                                Text="跟随光标" />
                                            <TextBlock
                                                Margin="0,4,0,0"
                                                FontSize="11"
                                                Foreground="{DynamicResource SecondaryTextBrush}"
                                                Text="窗口显示在文本光标附近"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>

                                    <Border
                                        Grid.Column="1"
                                        Margin="4,0,4,0"
                                        Cursor="Hand">
                                        <Border.Style>
                                            <Style BasedOn="{StaticResource ThemeCardStyle}" TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFollowMouse}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                                        <Setter Property="BorderThickness" Value="2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.InputBindings>
                                            <MouseBinding Command="{Binding SelectFollowMouseCommand}" MouseAction="LeftClick" />
                                        </Border.InputBindings>
                                        <StackPanel Margin="12">
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="Medium"
                                                Foreground="{DynamicResource PrimaryTextBrush}"
                                                Text="跟随鼠标" />
                                            <TextBlock
                                                Margin="0,4,0,0"
                                                FontSize="11"
                                                Foreground="{DynamicResource SecondaryTextBrush}"
                                                Text="窗口显示在鼠标指针附近"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>

                                    <Border
                                        Grid.Column="2"
                                        Margin="8,0,0,0"
                                        Cursor="Hand">
                                        <Border.Style>
                                            <Style BasedOn="{StaticResource ThemeCardStyle}" TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsScreenCenter}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                                        <Setter Property="BorderThickness" Value="2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.InputBindings>
                                            <MouseBinding Command="{Binding SelectScreenCenterCommand}" MouseAction="LeftClick" />
                                        </Border.InputBindings>
                                        <StackPanel Margin="12">
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="Medium"
                                                Foreground="{DynamicResource PrimaryTextBrush}"
                                                Text="屏幕中心" />
                                            <TextBlock
                                                Margin="0,4,0,0"
                                                FontSize="11"
                                                Foreground="{DynamicResource SecondaryTextBrush}"
                                                Text="窗口固定在当前屏幕中央"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>

                            <!--  剪贴项最大高度设置  -->
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,0,16,0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="剪贴项最大高度" />

                                <Slider
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    IsSnapToTickEnabled="True"
                                    Maximum="500"
                                    Minimum="50"
                                    TickFrequency="10"
                                    Value="{Binding ClipboardItemMaxHeight}" />

                                <TextBlock
                                    Grid.Column="2"
                                    Width="60"
                                    Margin="16,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    FontWeight="Medium"
                                    Foreground="{DynamicResource PrimaryBrush}"
                                    Text="{Binding ClipboardItemMaxHeight, StringFormat={}{0}px}" />
                            </Grid>

                            <TextBlock
                                Margin="0,8,0,0"
                                FontSize="12"
                                Foreground="{DynamicResource PrimaryTextBrush}"
                                Text="💡 设置剪贴列表中每个项目显示的最大高度，范围：50-500 像素" />

                            <Separator Margin="0,16,0,16" />

                            <!--  历史记录上限设置  -->
                            <Grid Margin="0,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,0,16,0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="历史记录上限" />

                                <Slider
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    IsSnapToTickEnabled="True"
                                    Maximum="10000"
                                    Minimum="100"
                                    TickFrequency="100"
                                    Value="{Binding HistoryLimit}" />

                                <TextBlock
                                    Grid.Column="2"
                                    Width="60"
                                    Margin="16,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    FontWeight="Medium"
                                    Foreground="{DynamicResource PrimaryBrush}"
                                    Text="{Binding HistoryLimit, StringFormat={}{0}条}" />
                            </Grid>

                            <TextBlock
                                Margin="0,8,0,0"
                                FontSize="12"
                                Foreground="{DynamicResource PrimaryTextBrush}"
                                Text="💡 设置剪贴板历史记录的最大存储数量，收藏项不受此限制" />
                        </StackPanel>
                    </Border>

                    <!--  关于信息卡片  -->
                    <Border Style="{StaticResource SettingsCardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SettingsTitleStyle}" Text="关于" />

                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Margin="0,0,16,8"
                                    FontSize="13"
                                    Foreground="{DynamicResource SecondaryTextBrush}"
                                    Text="应用名称" />
                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Margin="0,0,0,8"
                                    FontSize="13"
                                    FontWeight="Medium"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="ClipMate" />

                                <TextBlock
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Margin="0,0,16,8"
                                    FontSize="13"
                                    Foreground="{DynamicResource SecondaryTextBrush}"
                                    Text="版本号" />
                                <TextBlock
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Margin="0,0,0,8"
                                    FontSize="13"
                                    FontWeight="Medium"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="{Binding Version}" />

                                <TextBlock
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    Margin="0,0,16,0"
                                    FontSize="13"
                                    Foreground="{DynamicResource SecondaryTextBrush}"
                                    Text="描述" />
                                <TextBlock
                                    Grid.Row="2"
                                    Grid.Column="1"
                                    FontSize="13"
                                    Foreground="{DynamicResource PrimaryTextBrush}"
                                    Text="一个简洁高效的剪贴板管理工具"
                                    TextWrapping="Wrap" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
